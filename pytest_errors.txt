============================= test session starts ==============================
platform darwin -- Python 3.12.1, pytest-9.0.3, pluggy-1.6.0 -- /Users/vin8003/work/ordereasy/.venv/bin/python
cachedir: .pytest_cache
django: version: 5.2.9, settings: ordering_platform.settings (from ini)
rootdir: /Users/vin8003/work/ordereasy/RetailerCustomerPlatform
configfile: pytest.ini
plugins: mock-3.15.1, Faker-40.15.0, cov-7.1.0, anyio-4.12.0, xdist-3.8.0, django-4.12.0
collecting ... collected 17 items

authentication/tests/test_utils.py::TestAuthenticationUtils::test_verify_firebase_id_token_success PASSED [  5%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_verify_firebase_id_token_value_error PASSED [ 11%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_verify_firebase_id_token_dev_fallback PASSED [ 17%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_verify_firebase_id_token_general_exception PASSED [ 23%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_verify_otp_helper PASSED [ 29%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_send_sms_otp_thread PASSED [ 35%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_send_sms_otp FAILED [ 41%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_send_email_otp_thread PASSED [ 47%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_send_email_otp FAILED [ 52%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_is_valid_phone_number PASSED [ 58%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_generate_username_from_phone PASSED [ 64%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_rate_limit_user FAILED [ 70%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_log_security_event PASSED [ 76%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_get_client_ip PASSED [ 82%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_mask_phone_number FAILED [ 88%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_create_session_token PASSED [ 94%]
authentication/tests/test_utils.py::TestAuthenticationUtils::test_hash_token PASSED [100%]

=================================== FAILURES ===================================
__________________ TestAuthenticationUtils.test_send_sms_otp ___________________

args = (<authentication.tests.test_utils.TestAuthenticationUtils object at 0x110df3800>,)
keywargs = {'override_authkey_settings': <pytest_django.fixtures.SettingsWrapper object at 0x110fd6900>}

    @wraps(func)
    def patched(*args, **keywargs):
>       with self.decoration_helper(patched,
                                    args,
                                    keywargs) as (newargs, newkeywargs):

../../../.pyenv/versions/3.12.1/lib/python3.12/unittest/mock.py:1384: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../../../.pyenv/versions/3.12.1/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
../../../.pyenv/versions/3.12.1/lib/python3.12/unittest/mock.py:1366: in decoration_helper
    arg = exit_stack.enter_context(patching)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../.pyenv/versions/3.12.1/lib/python3.12/contextlib.py:526: in enter_context
    result = _enter(cm)
             ^^^^^^^^^^
../../../.pyenv/versions/3.12.1/lib/python3.12/unittest/mock.py:1439: in __enter__
    self.target = self.getter()
                  ^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

name = 'authentication.utils.threading'

    def resolve_name(name):
        """
        Resolve a name to an object.
    
        It is expected that `name` will be a string in one of the following
        formats, where W is shorthand for a valid Python identifier and dot stands
        for a literal period in these pseudo-regexes:
    
        W(.W)*
        W(.W)*:(W(.W)*)?
    
        The first form is intended for backward compatibility only. It assumes that
        some part of the dotted name is a package, and the rest is an object
        somewhere within that package, possibly nested inside other objects.
        Because the place where the package stops and the object hierarchy starts
        can't be inferred by inspection, repeated attempts to import must be done
        with this form.
    
        In the second form, the caller makes the division point clear through the
        provision of a single colon: the dotted name to the left of the colon is a
        package to be imported, and the dotted name to the right is the object
        hierarchy within that package. Only one import is needed in this form. If
        it ends with the colon, then a module object is returned.
    
        The function will return an object (which might be a module), or raise one
        of the following exceptions:
    
        ValueError - if `name` isn't in a recognised format
        ImportError - if an import failed when it shouldn't have
        AttributeError - if a failure occurred when traversing the object hierarchy
                         within the imported package to get to the desired object.
        """
        global _NAME_PATTERN
        if _NAME_PATTERN is None:
            # Lazy import to speedup Python startup time
            import re
            dotted_words = r'(?!\d)(\w+)(\.(?!\d)(\w+))*'
            _NAME_PATTERN = re.compile(f'^(?P<pkg>{dotted_words})'
                                       f'(?P<cln>:(?P<obj>{dotted_words})?)?$',
                                       re.UNICODE)
    
        m = _NAME_PATTERN.match(name)
        if not m:
            raise ValueError(f'invalid format: {name!r}')
        gd = m.groupdict()
        if gd.get('cln'):
            # there is a colon - a one-step import is all that's needed
            mod = importlib.import_module(gd['pkg'])
            parts = gd.get('obj')
            parts = parts.split('.') if parts else []
        else:
            # no colon - have to iterate to find the package boundary
            parts = name.split('.')
            modname = parts.pop(0)
            # first part *must* be a module/package.
            mod = importlib.import_module(modname)
            while parts:
                p = parts[0]
                s = f'{modname}.{p}'
                try:
                    mod = importlib.import_module(s)
                    parts.pop(0)
                    modname = s
                except ImportError:
                    break
        # if we reach this point, mod is the module, already imported, and
        # parts is the list of parts in the object hierarchy to be traversed, or
        # an empty list if just the module is wanted.
        result = mod
        for p in parts:
>           result = getattr(result, p)
                     ^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'authentication.utils' has no attribute 'threading'

../../../.pyenv/versions/3.12.1/lib/python3.12/pkgutil.py:528: AttributeError
_________________ TestAuthenticationUtils.test_send_email_otp __________________

args = (<authentication.tests.test_utils.TestAuthenticationUtils object at 0x110f2c1d0>,)
keywargs = {}

    @wraps(func)
    def patched(*args, **keywargs):
>       with self.decoration_helper(patched,
                                    args,
                                    keywargs) as (newargs, newkeywargs):

../../../.pyenv/versions/3.12.1/lib/python3.12/unittest/mock.py:1384: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
../../../.pyenv/versions/3.12.1/lib/python3.12/contextlib.py:137: in __enter__
    return next(self.gen)
           ^^^^^^^^^^^^^^
../../../.pyenv/versions/3.12.1/lib/python3.12/unittest/mock.py:1366: in decoration_helper
    arg = exit_stack.enter_context(patching)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
../../../.pyenv/versions/3.12.1/lib/python3.12/contextlib.py:526: in enter_context
    result = _enter(cm)
             ^^^^^^^^^^
../../../.pyenv/versions/3.12.1/lib/python3.12/unittest/mock.py:1439: in __enter__
    self.target = self.getter()
                  ^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

name = 'authentication.utils.threading'

    def resolve_name(name):
        """
        Resolve a name to an object.
    
        It is expected that `name` will be a string in one of the following
        formats, where W is shorthand for a valid Python identifier and dot stands
        for a literal period in these pseudo-regexes:
    
        W(.W)*
        W(.W)*:(W(.W)*)?
    
        The first form is intended for backward compatibility only. It assumes that
        some part of the dotted name is a package, and the rest is an object
        somewhere within that package, possibly nested inside other objects.
        Because the place where the package stops and the object hierarchy starts
        can't be inferred by inspection, repeated attempts to import must be done
        with this form.
    
        In the second form, the caller makes the division point clear through the
        provision of a single colon: the dotted name to the left of the colon is a
        package to be imported, and the dotted name to the right is the object
        hierarchy within that package. Only one import is needed in this form. If
        it ends with the colon, then a module object is returned.
    
        The function will return an object (which might be a module), or raise one
        of the following exceptions:
    
        ValueError - if `name` isn't in a recognised format
        ImportError - if an import failed when it shouldn't have
        AttributeError - if a failure occurred when traversing the object hierarchy
                         within the imported package to get to the desired object.
        """
        global _NAME_PATTERN
        if _NAME_PATTERN is None:
            # Lazy import to speedup Python startup time
            import re
            dotted_words = r'(?!\d)(\w+)(\.(?!\d)(\w+))*'
            _NAME_PATTERN = re.compile(f'^(?P<pkg>{dotted_words})'
                                       f'(?P<cln>:(?P<obj>{dotted_words})?)?$',
                                       re.UNICODE)
    
        m = _NAME_PATTERN.match(name)
        if not m:
            raise ValueError(f'invalid format: {name!r}')
        gd = m.groupdict()
        if gd.get('cln'):
            # there is a colon - a one-step import is all that's needed
            mod = importlib.import_module(gd['pkg'])
            parts = gd.get('obj')
            parts = parts.split('.') if parts else []
        else:
            # no colon - have to iterate to find the package boundary
            parts = name.split('.')
            modname = parts.pop(0)
            # first part *must* be a module/package.
            mod = importlib.import_module(modname)
            while parts:
                p = parts[0]
                s = f'{modname}.{p}'
                try:
                    mod = importlib.import_module(s)
                    parts.pop(0)
                    modname = s
                except ImportError:
                    break
        # if we reach this point, mod is the module, already imported, and
        # parts is the list of parts in the object hierarchy to be traversed, or
        # an empty list if just the module is wanted.
        result = mod
        for p in parts:
>           result = getattr(result, p)
                     ^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'authentication.utils' has no attribute 'threading'

../../../.pyenv/versions/3.12.1/lib/python3.12/pkgutil.py:528: AttributeError
_________________ TestAuthenticationUtils.test_rate_limit_user _________________

self = <authentication.tests.test_utils.TestAuthenticationUtils object at 0x110f2d070>

    def test_rate_limit_user(self):
        from django.core.cache import cache
        cache.clear()
        assert rate_limit_user("test", max_attempts=2) is True
        assert rate_limit_user("test", max_attempts=2) is True
>       assert rate_limit_user("test", max_attempts=2) is False
E       AssertionError: assert True is False
E        +  where True = rate_limit_user('test', max_attempts=2)

authentication/tests/test_utils.py:119: AssertionError
________________ TestAuthenticationUtils.test_mask_phone_number ________________

self = <authentication.tests.test_utils.TestAuthenticationUtils object at 0x110f2e000>

    def test_mask_phone_number(self):
>       assert mask_phone_number("+1234567890") == "+123**7890"
E       AssertionError: assert '+123***7890' == '+123**7890'
E         
E         - +123**7890
E         + +123***7890
E         ?       +

authentication/tests/test_utils.py:136: AssertionError
================================ tests coverage ================================
_______________ coverage: platform darwin, python 3.12.1-final-0 _______________

Coverage HTML written to dir htmlcov
=========================== short test summary info ============================
FAILED authentication/tests/test_utils.py::TestAuthenticationUtils::test_send_sms_otp
FAILED authentication/tests/test_utils.py::TestAuthenticationUtils::test_send_email_otp
FAILED authentication/tests/test_utils.py::TestAuthenticationUtils::test_rate_limit_user
FAILED authentication/tests/test_utils.py::TestAuthenticationUtils::test_mask_phone_number
========================= 4 failed, 13 passed in 2.55s =========================
