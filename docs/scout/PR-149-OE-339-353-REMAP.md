# FREEZE: PR #149 draft labels OE-339–353 → remap to OE-367+

**Status:** docs-only remap (2026-09-18). Not minted. Not product work.

Scout PR **[#149](https://github.com/vin8003/RetailerCustomerPlatform/pull/149)** (`feature/oe-thin-backlog-scout-r2-1c57`) labelled fifteen thin slices `OE-339`–`OE-353`. Those numbers were **draft labels**, but Jira already has live issues on every key in that range. **Do not remint over OE-339–353. Do not build the #149 drafts under those numbers.**

## Builder lock (read this first)

Builders **must not** launch `OE-339`–`OE-353` as the #149 draft collisions. Those keys are occupied by live Jira titles (see [Live occupants](#live-occupants-do-not-reuse)).

Use the **proposed remapped keys** in the table below **only after this remap lands**. If Jira mints past `OE-366` before mint, re-verify the next free keys and keep the file slices — do not force `339`–`353`.

- Dummy / local only. Never `*.ordereasy.win`.
- This PR is **draft**. Do not merge it as a deploy. Do not flip Jira to Done.
- Do not implement product code from this folder.

Related scout backlog (still draft, do not merge as a mint source for 339–353): [#149](https://github.com/vin8003/RetailerCustomerPlatform/pull/149), earlier [#143](https://github.com/vin8003/RetailerCustomerPlatform/pull/143).

## Jira verification (unused keys)

Checked on **2026-09-18** against Atlassian site `https://vin8003.atlassian.net` (cloud id `e13a41e0-528b-4326-800a-5724ff458b62`):

| Check | Result |
|-------|--------|
| Highest existing OE key | **OE-366** (`Thin BE: optional is_serialized on product serializer if field exists`, Need Clarity) |
| `OE-339`–`OE-366` | All exist (live issues) |
| `getJiraIssue` `OE-367` … `OE-381` | Unused (`Issue does not exist`) |
| Next free block for this remap | **OE-367**–**OE-380** (14 proposed keys) |

Proposed keys are **unused labels for a later mint**. This PR does not create Jira issues.

## Remap table

Format: old #149 draft label → proposed new key → one-line slice title → repo hint.

| Old draft label | Proposed new key | One-line slice title | Repo hint |
|-----------------|------------------|----------------------|-----------|
| OE-339 | **OE-367** | FE — optional `unit` on PurchaseReturnModal picker | `retailer_ordereasy_njs` · `src/components/dashboard/PurchaseReturnModal.tsx` |
| OE-340 | **OE-368** | FE — read-only `saleable_quantity` hint on ProductForm | `retailer_ordereasy_njs` · `src/components/products/ProductForm.tsx` |
| OE-341 | **OE-369** | FE customer — optional `delivery_charge` on shop header | `customer_ordereasy_njs` · `src/app/retailer/page.tsx` (header only) |
| OE-342 | **OE-370** | FE — optional `unit` on POSReturnModal picker | `retailer_ordereasy_njs` · `src/components/pos/POSReturnModal.tsx` |
| OE-343 | **OE-371** | F follow-on — `brand_name` on wishlist items | `RetailerCustomerPlatform` · `customers/serializers.py` (`CustomerWishlistSerializer`) |
| OE-344 | — skipped | F follow-on — `unit` on purchase-return items | Already live as **OE-355** (same serializer slice) |
| OE-345 | **OE-372** | F follow-on — `unit` on purchase-return `get_invoice_items` | `RetailerCustomerPlatform` · `returns/views.py` (`PurchaseReturnViewSet.get_invoice_items`) |
| OE-346 | **OE-373** | FE customer — optional featured / seasonal on ProductCard | `customer_ordereasy_njs` · `src/app/components/ProductCard.tsx` |
| OE-347 | **OE-374** | FE customer — optional featured / seasonal on PDP | `customer_ordereasy_njs` · `src/app/retailer/product/page.tsx` |
| OE-348 | **OE-375** | F follow-on — `barcode` on cart items | `RetailerCustomerPlatform` · `cart/serializers.py` (`CartItemSerializer`) |
| OE-349 | **OE-376** | F follow-on — `barcode` on purchase invoice line items | `RetailerCustomerPlatform` · `products/serializers.py` (`PurchaseItemSerializer`) |
| OE-350 | **OE-377** | F follow-on — `unit` on inventory-ledger rows | `RetailerCustomerPlatform` · `products/api_erp_views.py` (`get_inventory_ledger`) |
| OE-351 | **OE-378** | FE — optional product identity on inventory ledger | `retailer_ordereasy_njs` · `src/app/dashboard/products/ledger/page.tsx` |
| OE-352 | **OE-379** | F follow-on — `brand_name` on order line items | `RetailerCustomerPlatform` · `orders/serializers.py` (`OrderItemSerializer`) |
| OE-353 | **OE-380** | FE — optional `unit` on purchase return-detail lines | `retailer_ordereasy_njs` · `src/app/dashboard/purchases/return-detail/page.tsx` |

## Skipped (already delivered under a live key)

Do not assign a new key for these titles. Do not remint them onto `OE-339`–`OE-353`.

| #149 draft label | Draft title | Live key (keep) | Why skip |
|------------------|-------------|-----------------|----------|
| *(not a #149 title)* | optional `hsn_code` on purchase-return items | **OE-339** (In Review) | Live occupant of the frozen number; example collision. #149’s *label* OE-339 was PurchaseReturnModal `unit` — that **title** remaps to **OE-367**. |
| OE-344 | F follow-on — `unit` on purchase-return items | **OE-355** (In Review) | Same title and slice (`PurchaseReturnItemSerializer` `unit` echo). Live AC already excludes `get_invoice_items` (that remains **OE-372**). |

Adjacent live work that is **not** the same slice (do not skip the remap):

- OE-288 — `saleable_quantity` on POS/catalog display ≠ ProductForm hint (**OE-368**).
- OE-296 / OE-294 / OE-299 — retailer catalog/search featured/seasonal ≠ customer ProductCard/PDP (**OE-373**, **OE-374**).
- OE-314 — cart `brand_name` ≠ cart `barcode` (**OE-375**).
- OE-315 — BE ledger product identity ≠ FE ledger identity line (**OE-378**).
- OE-316 — customer ProductCard `brand_name` ≠ featured/seasonal badges (**OE-373**).
- OE-332 — FE wishlist `brand_name` row ≠ BE wishlist serializer (**OE-371**).
- OE-345 — FE `warehouse_name` on ledger ≠ BE ledger `unit` (**OE-377**) and ≠ FE identity line (**OE-378**).

## Live occupants (do not reuse)

Frozen numbers and the **actual** Jira summaries as of 2026-09-18. Implementing a #149 draft against these keys is a collision.

| Live key | Live Jira summary | Status (check day) |
|----------|-------------------|--------------------|
| OE-339 | F follow-on — optional hsn_code on purchase-return items | In Review |
| OE-340 | FE customer — optional barcode on product detail | In Review |
| OE-341 | FE customer — optional category_name on product detail | In Review |
| OE-342 | FE customer — optional pack_size on cart line | In Review |
| OE-343 | F follow-on — optional barcode on sales-return items | In Review |
| OE-344 | FE customer — optional hsn_code on product detail | In Review |
| OE-345 | FE — warehouse_name on inventory ledger | In Review |
| OE-346 | FE customer — optional delivery_slot_label on order detail | In Review |
| OE-347 | FE customer — optional warehouse_name on order detail | Need Clarity |
| OE-348 | FE — optional po_number on purchase invoice list | In Review |
| OE-349 | FE — optional rack_location on inventory adjust history | In Review |
| OE-350 | FE customer — optional loyalty_points on order detail | In Review |
| OE-351 | FE — optional salesman_name on retailer order detail | In Review |
| OE-352 | F follow-on — brand_name on purchase-return items | Need Clarity |
| OE-353 | FE customer — optional mrp strikethrough on product list row | Need Clarity |

`OE-354`–`OE-366` are also live. Do not use them for #149 leftovers.

## Out of scope for this doc

- No product code, no new Jira mint, no status change, no live hosts.
- No rewrite of #149 `docs/oe-thin-backlog/` (that branch still carries the colliding draft labels).
- No merge. Leave this PR draft until a human approves a later mint using **OE-367+** only.
