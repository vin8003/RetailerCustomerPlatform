---
id: OE-302
title: F follow-on — is_featured / is_available / is_in_stock on POS no_page
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/pos-nopage-availability-flags.md
jira: https://vin8003.atlassian.net/browse/OE-302
---

# OE-302 F follow-on — `is_featured` / `is_available` / `is_in_stock` on POS no_page

Work snapshot. Durable: [pos-nopage-availability-flags.md](../requirements/pos-nopage-availability-flags.md).

List/detail serializers already expose top-level `Product.is_featured`, `Product.is_available`, and `Product.is_in_stock`. POS `GET /api/products/?no_page=true` built a lightweight row dict that omitted them. This slice only echoes those three bools on the POS `no_page` row.

`ProductSearchSerializer` Meta is untouched (owned by other tickets).

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| POS `no_page` row includes `is_featured`, `is_available`, `is_in_stock` | List/detail only | **EXTEND** — same model / property values |
| False stays false | List/detail pass through | same on POS (do not omit) |
| Auth/tenancy / search Meta | Shop-scoped retailer GET | unchanged; search serializer not edited |
| Featured / availability write / FE | Existing / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- POS `no_page` hand-built dict includes `is_featured`, `is_available`, `is_in_stock`
- Matches list/detail for the same SKU
- Tests: true/false echo vs list/detail, `is_in_stock` tracked + untracked, search Meta lock, auth, tenancy

Out of scope: search Meta, featured/availability write, FE, pack write, inventory.adjust, timeline/OFD/khata/UPI/slots, merge, Jira Done, live `*.ordereasy.win`.
