---
id: OE-286
title: F follow-on — top-level unit on POS no_page
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/pos-nopage-unit.md
jira: https://vin8003.atlassian.net/browse/OE-286
---

# OE-286 F follow-on — top-level `unit` on POS no_page

Work snapshot. Durable: [pos-nopage-unit.md](../requirements/pos-nopage-unit.md).

List/detail/search serializers already expose `Product.unit`. POS `GET /api/products/?no_page=true` built a lightweight row dict that omitted it. CTO overnight lock is this EXTEND only: echo the same top-level `unit` on the POS `no_page` row.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| POS `no_page` row includes `unit` | List/detail/search only | **EXTEND** — same model value |
| Null/empty `unit` stays null/empty | List/detail pass through | same on POS (no invented `piece`) |
| Auth/tenancy / public catalog | Shop-scoped retailer GET | unchanged |
| Unit conversion / write APIs | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- POS `no_page` hand-built dict includes `unit` from `Product.unit`
- Matches list/detail for the same SKU
- Tests: parity with list/detail, empty passthrough, auth, tenancy, public unchanged, OE-285 pack identity + OE-284 margin non-regression

Out of scope: unit-conversion engines, pack write, inventory.adjust, timeline/OFD/khata/UPI/slots, FE, merge, Jira Done, live `*.ordereasy.win`.
