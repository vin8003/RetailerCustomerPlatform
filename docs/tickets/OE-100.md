---
id: OE-100
title: F-0041 — Suppliers
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/suppliers.md
  - docs/requirements/shop-staff-roles.md
jira: https://vin8003.atlassian.net/browse/OE-100
---

# OE-100 F-0041 — Suppliers (thin slice)

Work snapshot. Durable: [suppliers.md](../requirements/suppliers.md).

Problem: maintain suppliers (contacts, tax ids, payment terms) as the counterparty for POs and GRNs. EXTEND current supplier khata parties — do not invent a second vendor master.

Hard dep F-0001 (org/tenancy) is assumed on the stacked tip.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Create supplier with GSTIN optional | **EXISTING** (`gst_number` blank=True). No format/org-duplicate check. | **EXTEND** — optional create kept; invalid format 400; org duplicate flagged |
| Duplicate GSTIN flagged per org | **MISSING** | **EXTEND** — 400 + `gstin_duplicate` |
| Inactive suppliers cannot be selected on new POs | `is_active` **EXISTING**; **no PO create**; purchase-invoice create did not check | **EXTEND** — gate on purchase-invoice create / supplier change; PO picker hook documented for OE-102 |
| Unauthorized cannot change payment terms (403) | **MISSING** (no field, no perm) | **EXTEND** — `payment_terms` + `purchasing.terms` |
| Tenant A cannot read/mutate tenant B | Shop-owner queryset only; staff without a profile 500 | **EXTEND** — org-scoped queryset, 404 deny |
| Full PO/GRN three-way match | N/A | **OUT** (OE-102) |

`products.SupplierLedger` is already used by purchase invoices — not replaced.

## Done in this change (EXTEND, one slice)

- `payment_terms` on `retailers.Supplier`
- Optional GSTIN create; duplicate non-blank GSTIN flagged per org
- `purchasing.terms` (catalog v10); cashier **403**, resource unchanged
- Inactive supplier blocked on new purchase invoices (current inward path)
- `?is_active=true` + `assert_supplier_selectable_for_new_purchase` for OE-102
- Org tenancy on supplier CRUD
- Tests: optional GSTIN, duplicate flag, inactive PI, payment-terms 403, tenancy
- SHIP nits: org-unique GSTIN DB constraint (denormalized `organization` + partial unique; race → same 400), null/blank GSTIN normalize to `''`, `payment_terms` trim + reject whitespace-only, `assertNumQueries` list **4** / create **8**

Out of scope: OE-102 PO/GRN engine, bank payments, buyer analytics, FE redesign, Jira Done, live `*.ordereasy.win`.
