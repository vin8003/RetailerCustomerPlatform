---
id: OE-294
title: F follow-on — is_seasonal on retailer search
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/search-is-seasonal.md
jira: https://vin8003.atlassian.net/browse/OE-294
---

# OE-294 F follow-on — `is_seasonal` on retailer search

Work snapshot. Durable: [search-is-seasonal.md](../requirements/search-is-seasonal.md).

List/detail serializers and POS `no_page` already expose top-level `Product.is_seasonal`. `ProductSearchSerializer` Meta.fields omitted it after OE-287/290/291/293 (`brand_name` + `barcode` + `category_name` + `original_price`). This slice only returns top-level `is_seasonal` on search.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Search row includes `is_seasonal` | List/detail/POS only | **EXTEND** — same model value |
| False stays false | List/detail/POS pass through | same on search (do not omit) |
| Auth/tenancy / public catalog | Shop-scoped retailer GET | unchanged; public search inherits via shared serializer |
| Seasonal write / policy | Existing / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- `ProductSearchSerializer` exposes `is_seasonal` (`Product.is_seasonal`) the same way list does
- Tests: search parity with list/detail/POS, true and false both echo, public list + public search, OE-287 `brand_name` + OE-290 `barcode` + OE-291 `category_name` + OE-293 `original_price` light non-regression, auth denied, shop-scoped tenancy

Out of scope: seasonal write/policy, FE, OE-102, GST rebuild, inventory.adjust, timeline/OFD/khata/UPI/slots, pack write, merge, Jira Done, live `*.ordereasy.win`.
