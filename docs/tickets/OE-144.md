---
id: OE-144
title: F-0067 — Web POS checkout
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/product-batch-expiry.md
jira: https://vin8003.atlassian.net/browse/OE-144
---

# OE-144 F-0067 — POS no_page batch expiry_date (thin slice)

Work snapshot. Durable: [product-batch-expiry.md](../requirements/product-batch-expiry.md).

Jira card is the full web POS checkout. CTO overnight lock is this EXTEND only: POS `GET /api/products/?no_page=true` active batch rows include `expiry_date` (null allowed), matching `ProductBatchSerializer`. Tenancy/auth unchanged. No new expiry policy.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| POS `no_page` active batches show expiry | Hand-built batch dict omitted `expiry_date` | **EXTEND** — same optional date as `ProductBatchSerializer` |
| Null expiry allowed | Serializer already null-OK | **EXTEND** — POS echoes null |
| Tenant A cannot read tenant B | Shop-scoped product get | unchanged |
| Unauthenticated / customer denied | 401 / 403 | unchanged |
| Expiry alerts, notify, FE dashboards, new sale policy | Missing / other tickets | still out of scope |

## Done in this change (EXTEND, one slice)

- `expiry_date` on POS `no_page` active batch rows
- Reuses stored `ProductBatch.expiry_date`; no new service or policy
- Tests: dated, null, inactive omitted, tenancy, auth

Out of scope: full F-0067 checkout, expiry alerts/notify, FE dashboards, OE-102, #103, inventory policy invent, Jira Done, live `*.ordereasy.win`.
