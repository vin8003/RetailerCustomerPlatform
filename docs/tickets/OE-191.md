---
id: OE-191
title: F-0019 — Kit and assembly products
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/pack-children-reads.md
  - docs/requirements/parent-child-pack-skus.md
  - docs/requirements/saleable-quantity-reads.md
jira: https://vin8003.atlassian.net/browse/OE-191
---

# OE-191 F-0019 — pack children on parent SKU reads (thin slice)

Work snapshot. Durable: [pack-children-reads.md](../requirements/pack-children-reads.md).

Jira card is the full kit/BOM + assemble capability. CTO overnight lock is this EXTEND only: retailer GET of a parent SKU returns active `fractional_children` (`id`, `name`, `conversion_factor`, `saleable_quantity`). Non-parent → `[]`. Cross-tenant → **404**. No BOM / assemble write.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Parent SKU retailer reads show active pack children | Related name + pack flags exist; no children array | **EXTEND** — `fractional_children` on list/detail |
| Non-parent → `[]` | N/A | **EXTEND** |
| Tenant A cannot read tenant B | Shop-scoped product gets | **EXTEND** — detail **404** |
| Kit/BOM tables / assemble write | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- `fractional_children` on retailer product detail and paginated list
- Reuses `Product.fractional_children` + `saleable_quantity()`; no new service layer
- Prefetch on those reads (no per-parent child query)
- Tests: parent with children, non-parent empty, tenancy, auth

Search + POS `no_page` children follow-on: [OE-283](OE-283.md). Pack identity scalars on those surfaces: [OE-285](OE-285.md).

Out of scope: full F-0019, OE-159, explode-at-sale, OE-102, #103, notify, FE redesign, Jira Done, live `*.ordereasy.win`.
