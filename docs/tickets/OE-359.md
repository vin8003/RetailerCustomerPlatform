---
id: OE-359
title: Thin BE — optional max_order_qty on product serializer
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/product-max-order-qty.md
jira: https://vin8003.atlassian.net/browse/OE-359
---

# OE-359 Thin BE — optional `max_order_qty` on product serializer

Work snapshot. Durable: [product-max-order-qty.md](../requirements/product-max-order-qty.md).

List/detail already expose `maximum_order_quantity`. Product has no `max_order_qty` column. This slice only echoes `max_order_qty` when the instance has that attribute. Do not add it to serializer Meta.fields. Do not use model Meta to detect it.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Instance with `max_order_qty` | Key omitted | **EXTEND** — key present (null stays null) |
| Instance without `max_order_qty` | n/a | key omitted; no crash |
| Meta.fields / model `_meta` | n/a | unchanged / unused |
| POS / writes / cart / orders | Existing / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- `OptionalMaxOrderQtyMixin` on list / detail / search
- Dummy tests: present, absent, null, decimal JSON shape; Meta.fields stay clean

Out of scope: model/migration, POS `no_page`, cart, orders, write serializers, FE, merge, Jira Done, live `*.ordereasy.win`.
