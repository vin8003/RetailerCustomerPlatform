---
id: OE-118
title: F-0046 — Purchase margin guard
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/purchase-margin-preview.md
  - docs/requirements/shop-staff-roles.md
jira: https://vin8003.atlassian.net/browse/OE-118
---

# OE-118 F-0046 — purchase margin% preview (thin slice)

Work snapshot. Durable: [purchase-margin-preview.md](../requirements/purchase-margin-preview.md).

Jira card is the full inbound margin guard (threshold policy, override audit, PO/GRN block). CTO overnight lock is this EXTEND only: purchase-role GET of margin% for a SKU from selling price vs draft-or-last PI cost.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Purchase-role sees margin% for a SKU | **MISSING** (price + PI cost exist; no preview read) | **EXTEND** — GET margin preview |
| Cost = draft SKU `purchase_price`, else last PI | Fields exist | **EXTEND** — draft then last PI |
| Missing cost → null, not 0 | N/A | **EXTEND** — `cost` / `margin_percent` null |
| Users without purchase permission cannot see cost/margin | Product reads already expose `purchase_price` | **EXTEND** — this preview is 403 without purchase-role |
| Tenant A cannot read tenant B | Product already org-scoped | **EXTEND** — org-scoped SKU + 404 |

## Done in this change (EXTEND, one slice)

- `GET /api/products/erp/products/<id>/margin-preview/`
- `selling_price` = `Product.price`; cost = `Product.purchase_price` if set, else latest `PurchaseItem.purchase_price`
- `margin_percent` = `(sell - cost) / sell * 100`; missing cost → `null` (not `0`)
- Purchase-role gate (`purchasing.terms`; owner implicit admin)
- Tests: happy draft + last-PI fallback; null cost; stored 0 is real; non-purchase 403; no cross-tenant

Out of scope: full F-0046 (threshold policy, override audit, PO create block), OE-102, #103 GST, FE matrix, notify, Jira Done, live `*.ordereasy.win`.
