---
id: OE-343
title: F follow-on — optional barcode on sales-return items
knowledge_class: temporary
owning_repo: RetailerCustomerPlatform
durable_docs: []
jira: https://vin8003.atlassian.net/browse/OE-343
---

# OE-343 F follow-on — optional barcode on sales-return items

Work snapshot. This slice is a **gated no-op** on tip `9e697ed` (BE #147).

`Product.barcode` already exists. `SalesReturnItem` has no barcode column. OE-313 `unit` has **not** landed on this tip (`SalesReturnItemSerializer` still omits `unit`; PR #145 is open / hot). Per the gate, do not add a barcode echo onto a hot serializer.

## Gap list (scout)

| AC | On tip `9e697ed` | This slice |
|----|------------------|------------|
| Optional `barcode` on sales-return items | Not present | **no-op** — comment only |
| OE-313 `unit` on the same serializer | Not present (#145) | still out of this PR |
| Auth / tenancy | Shop-scoped 401 / 404 | unchanged |
| Return write policy | Existing | unchanged |

## Done in this change (no-op)

- Comment on `SalesReturnItemSerializer` records the deferral
- Tests lock current item keys (no `barcode`, no `unit`), write-ignore, auth, tenancy

Re-open the echo only after OE-313 `unit` is on the tip and this serializer is no longer hot.

Out of scope: implementing OE-313, purchase-return serializer, `search_order`, search Meta, POS views, cart, ledger, FE, merge, Jira Done, live `*.ordereasy.win`.
