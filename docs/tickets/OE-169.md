---
id: OE-169
title: F-0071 — POS margin guard
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/purchase-margin-preview.md
  - docs/requirements/shop-staff-roles.md
jira: https://vin8003.atlassian.net/browse/OE-169
---

# OE-169 F-0071 — purchase-role `margin_percent` on catalog reads (thin slice)

Work snapshot. Durable: [purchase-margin-preview.md](../requirements/purchase-margin-preview.md).

Jira card is the full POS margin guard (threshold policy, till block, PIN/override). CTO overnight lock is this EXTEND only: purchase-role GET of product list/detail (and search) includes `margin_percent` from existing OE-118 helpers.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Purchase-role sees margin% on catalog list/detail | Preview endpoint only (OE-118) | **EXTEND** — `margin_percent` on list/detail/search |
| Cost = draft SKU `purchase_price`, else last PI | Helpers exist | reuse `draft_or_last_pi_cost` + `selling_margin_percent` |
| Missing cost → null, not 0 | Preview does this | same on catalog |
| Users without purchase permission cannot see margin | Preview is 403 | **EXTEND** — omit field on list/detail (cashiers still see catalog) |
| Tenant A cannot read tenant B | Product already shop-scoped | unchanged (404 / absent) |

## Done in this change (EXTEND, one slice)

- Purchase-role retailer GET list / detail / search includes `margin_percent`
- Same OE-118 math; missing cost → `null`
- Non-purchase omits the field; POS `no_page` till payload unchanged
- Tests: purchase vs cashier, null cost, last-PI, tenancy, auth, no N+1

Out of scope: POS till block, PIN/override, threshold policy, FE redesign, merge, Jira Done, live `*.ordereasy.win`.
