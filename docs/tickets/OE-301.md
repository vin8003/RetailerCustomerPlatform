---
id: OE-301
title: F follow-on — delivery_fee + discount_amount on order list
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/order-list-fee-discount.md
jira: https://vin8003.atlassian.net/browse/OE-301
---

# OE-301 F follow-on — `delivery_fee` + `discount_amount` on order list

Work snapshot. Durable: [order-list-fee-discount.md](../requirements/order-list-fee-discount.md).

`OrderDetailSerializer` already exposes top-level `Order.delivery_fee` and `Order.discount_amount`. `OrderListSerializer` Meta.fields omitted them. This slice only returns those two keys on the list row (plain Meta echo).

Sibling of PR #130 (`d022f37`). Does not touch `products/views.py` (POS `no_page` / OE-302) or `ProductSearchSerializer` Meta.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| List row includes `delivery_fee` + `discount_amount` | Detail only | **EXTEND** — same model values |
| Null stays null | Detail passes through | same on list serializer (do not omit) |
| Auth/tenancy | Shop-scoped retailer GET | unchanged |
| Fee math / payment write / FE | Existing / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- `OrderListSerializer` exposes `delivery_fee` and `discount_amount` the same way detail does (plain Meta fields)
- Tests: present echo + null echo (Decimal-equal to detail), retailer current/history HTTP parity, auth denied, shop-scoped tenancy, search Meta + POS `no_page` untouched lock

Out of scope: fee math, payment write, FE, POS `no_page`, search Meta, inventory.adjust, timeline/OFD/khata/UPI/slots, pack write, merge, Jira Done, live `*.ordereasy.win`.
