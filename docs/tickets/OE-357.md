---
id: OE-357
title: F follow-on — optional is_returnable on product detail
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/product-detail-is-returnable.md
jira: https://vin8003.atlassian.net/browse/OE-357
---

# OE-357 F follow-on — optional `is_returnable` on product detail

Work snapshot. Durable: [product-detail-is-returnable.md](../requirements/product-detail-is-returnable.md).

Product detail may echo optional top-level `is_returnable` (bool) only if that attribute already exists on the product instance / `Product` model. Do not invent a column, migration, or return policy.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Detail includes `is_returnable` when attribute exists | Field not echoed | **EXTEND** — bool on `ProductDetailSerializer` |
| Missing field | n/a | key omitted (do not invent `false`) |
| Present `false` | n/a | stays `false` (do not omit) |
| Search Meta / list / POS | Unchanged | still out of scope |

## Done in this change (EXTEND, one slice)

- `ProductDetailSerializer` echoes `is_returnable` via `optional_is_returnable` when the attribute exists
- Tests: dummy true/false, missing omit, search/list Meta lock, retailer detail 401/403/404, public detail same omit rule

Out of scope: `ProductSearchSerializer` Meta, POS `products/views.py`, list/create/update serializers, model/migration invent, cart, returns, ledger, FE, merge, Jira Done, live `*.ordereasy.win`.
