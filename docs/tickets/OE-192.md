---
id: OE-192
title: F-0018 — Size-color variant products
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/group-variants-reads.md
  - docs/requirements/app-vs-pos-prices.md
jira: https://vin8003.atlassian.net/browse/OE-192
---

# OE-192 F-0018 — group_variants on list/search/POS (thin slice)

Work snapshot. Durable: [group-variants-reads.md](../requirements/group-variants-reads.md).

Jira card is the full size/color matrix generator + variant SKU invent. CTO overnight lock is this EXTEND only: when `product_group` is set, retailer/POS list, search, and `no_page` return the same sibling `group_variants` shape detail already returns. Empty group → `[]`. Shop-scoped. No N+1.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| List/search/POS show sibling variants | Detail-only `get_group_variants()` | **EXTEND** — same shape on list/search/`no_page` |
| Empty group → `[]` | Detail only | **EXTEND** |
| Shop-scoped siblings | Detail filters `retailer=` | **EXTEND** — same filter + cache key |
| No N+1 on multi-product list | Per-product sibling query on detail | **EXTEND** — one group cache / list serializer |
| Size/color matrix generator | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- `group_variants` on retailer list, search, and POS `no_page`
- Reuses detail sibling shape (`id`/`name`/`unit`/`price`/`qty` plus the extra keys detail already sent)
- One shop-scoped prefetch for list/search; same cache on POS `no_page`
- Tests: grouped list/search/POS, empty, tenancy, auth, query budget

Out of scope: full F-0018 matrix generator, variant SKU invent, OE-102, #103, FE redesign, notify, Jira Done, live production hosts.
