---
id: OE-305
title: F follow-on — email/notes/credit scalars on customer list
knowledge_class: temporary
owning_repo: RetailerCustomerPlatform
durable_docs: []
jira: https://vin8003.atlassian.net/browse/OE-305
---

# OE-305 F follow-on — customer list scalars

Work snapshot. Status lives in Jira. Implementation PR: [#141](https://github.com/vin8003/RetailerCustomerPlatform/pull/141).

Retailer customer **detail** already exposes `email`, `notes`, `credit_limit`, and `credit_due_days`. The list serializer omits them. OE-305 is the thin READ EXTEND that echoes those four scalars on the list row.

## Sibling no-op (this branch)

A narrower sibling asked only for optional `notes` on the list serializer if `RetailerCustomerMapping.notes` exists and the list field is not already declared.

`customers/serializers.py` is **contested** by #141 (list + detail scalars). Isolation rule: no wishlist/list/detail customer scalars until OE-305 SHIP.

This sibling therefore **does not** edit `customers/serializers.py` or `customers/views.py`. Tests lock the tip contract (model + detail have `notes`; list omits it) and auth/tenancy.

## Gap list (scout)

| AC | Tip `9e697ed` | OE-305 #141 | This sibling |
|----|---------------|-------------|--------------|
| List row includes `notes` | omitted | **EXTEND** | **no-op** (file contested) |
| Null notes stays null | detail only | list + detail | unchanged |
| Auth/tenancy | shop-scoped GET | unchanged | unchanged (locked) |
| Credit / email / khata write / FE | existing / invent | email + credit scalars; no write | still out of scope |

## Out of scope

Khata write, blacklist write, FE, invent balances, live `*.ordereasy.win`, merge, Jira Done.
