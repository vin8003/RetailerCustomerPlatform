---
id: OE-190
title: F-0017 — Product catalog (POS saleable gate)
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/pos-saleable-products.md
jira: https://vin8003.atlassian.net/browse/OE-190
---

# OE-190 F-0017 — Product catalog (thin slice)

Work snapshot. Durable: [pos-saleable-products.md](../requirements/pos-saleable-products.md).

Problem: customer cart already rejects inactive/unavailable items; POS checkout loaded by `id` + retailer only and could still sell them.

Done in this change (EXTEND, one slice):

- `create_pos_order` rejects `is_active=False` or `is_available=False` with 400 (name + id)
- Tenancy unchanged (retailer-scoped get); missing product / cross-tenant still not 201
- Tests: inactive, unavailable, mixed cart, happy path, query budget, cross-tenant deny

Out of scope: new SKU field, by-barcode endpoint, POS no_page→search rewrite, products.* RBAC, purchase_price strip, catalog UI rebuild, customer FE, live `*.ordereasy.win`.
