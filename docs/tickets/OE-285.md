---
id: OE-285
title: F-0019 follow-on — pack identity scalars on search + POS no_page
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/pack-children-reads.md
  - docs/requirements/parent-child-pack-skus.md
jira: https://vin8003.atlassian.net/browse/OE-285
---

# OE-285 F-0019 follow-on — pack identity on search + POS no_page

Work snapshot. Durable: [pack-children-reads.md](../requirements/pack-children-reads.md).

OE-191 shipped list/detail `fractional_children` plus pack identity on those surfaces. OE-283 added `fractional_children` on search + POS `no_page`. Pack identity scalars (`is_parent_bulk`, `parent_bulk_product`, `conversion_factor`) were still missing on search + POS `no_page`.

CTO overnight lock is this EXTEND only: retailer GET search and POS `?no_page=true` echo the same pack identity scalars already on list/detail. Non-pack SKUs keep existing defaults/nulls. Auth/tenancy unchanged. READ only.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Search + POS no_page show pack identity | List/detail only (OE-191) | **EXTEND** — same values/shape |
| Non-pack → `false` / `null` / `null` | N/A on those surfaces | **EXTEND** |
| Tenant A cannot read tenant B | Search/POS already shop-scoped | unchanged (absent; detail **404**) |
| OE-159 break-bulk / explode-at-sale | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- Pack identity scalars on retailer product search and POS `no_page`
- Reuses model fields; search serializer Meta + POS hand-built dict; no new service layer
- Parent pointer is the existing FK id (`parent_bulk_product_id`); search/POS `select_related` the parent so cache + identity do not fetch per child
- Tests: search, POS no_page, non-pack, tenancy, auth, compare to detail, query budget

Out of scope: full F-0019, OE-159, explode-at-sale, kit/BOM invent, notify, FE redesign, merge, Jira Done, live `*.ordereasy.win`.
