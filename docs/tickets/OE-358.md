---
id: OE-358
title: F follow-on — optional remarks on write-off list
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/write-off-list-remarks.md
  - docs/requirements/damage-expiry-write-off.md
jira: https://vin8003.atlassian.net/browse/OE-358
---

# OE-358 F follow-on — optional `remarks` on write-off list

Work snapshot. Durable: [write-off-list-remarks.md](../requirements/write-off-list-remarks.md).

Write-off / inventory-ledger rows already echo `reason` and product-list identity. They omitted optional free-text remarks. `ProductInventoryLog` has no `remarks` column on this stack — do not invent one. This slice only echoes `remarks` when the attribute exists; otherwise `null`.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Write-off list includes optional `remarks` | Reason / identity / qty only | **EXTEND** — `getattr(log, 'remarks', None)` |
| Missing field / null stays null | n/a | same (`null` / `""` passthrough) |
| Do not invent from `reason` | n/a | `remarks` is not copied from `reason` |
| Auth/tenancy / no N+1 | Shop-scoped GET | unchanged |
| Write-off POST persist / migration | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- `WriteOffListSerializer` exposes read-only `remarks` via `inventory_log_remarks`
- `GET /api/products/erp/inventory-ledger/` uses that serializer (same identity / qty / reason keys)
- Tests: missing-field null, attribute echo, null/empty passthrough, no invent from reason, write ignore, auth, tenancy, query budget, search Meta stays without `remarks`

Out of scope: `ProductInventoryLog` migration, write-off POST persist, FE write-off list (OE-326), ledger `unit` (OE-350), ProductSearchSerializer Meta, POS views, timeline/OFD/khata/UPI/slots, merge, Jira Done, live `*.ordereasy.win`.
