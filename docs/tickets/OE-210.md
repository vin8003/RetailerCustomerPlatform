---
id: OE-210
title: F-0125 — Sales, stock, and margin reports
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/expiry-batch-list.md
  - docs/requirements/product-batch-expiry.md
jira: https://vin8003.atlassian.net/browse/OE-210
---

# OE-210 F-0125 — expiry batch list (thin slice)

Work snapshot. Durable: [expiry-batch-list.md](../requirements/expiry-batch-list.md).

Jira card is the full E16 MIS (sales, purchase, stock, margin, slow movers). CTO overnight lock is this EXTEND only: retailer GET of this shop’s active on-hand batches with `expiry_date <= today+N` (default N=30).

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Shop list of batches expiring within N days | **MISSING** (dates on batch + product payload only) | **EXTEND** — GET list |
| Default window 30 days | N/A | **EXTEND** — omit `days` |
| Custom N | N/A | **EXTEND** — `?days=` |
| On-hand qty > 0 | N/A | **EXTEND** — filter |
| Include expired still on-hand | N/A | **EXTEND** — no lower date bound |
| Tenant A cannot read tenant B | Batches already shop-owned | **EXTEND** — shop + org queryset |

## Done in this change (EXTEND, one slice)

- `GET /api/products/erp/expiring-batches/`
- Reuses `ProductBatch` + `expiry_date`; no new service layer
- Tests: default N=30, custom N, qty>0, include expired on-hand, tenancy, auth

Out of scope: full F-0125 MIS, alerts, FE dashboards, `StockMovement` invent, OE-102, GST / #103, OE-149 notify, Jira Done, live `*.ordereasy.win`.
