---
id: OE-212
title: F-0105 — Customer profile and order history
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/customer-profile-order-history.md
jira: https://vin8003.atlassian.net/browse/OE-212
---

# OE-212 F-0105 — Customer profile and order history (thin slice)

Work snapshot. Durable: [customer-profile-order-history.md](../requirements/customer-profile-order-history.md).

Problem: one customer record with purchase history across POS and app. Omnichannel is a source, not a second CRM.

Done in this change (EXTEND, one slice):

- `GET /api/customer/retailer/lookup/?phone=` returns mapping/customer summary + last 20 POS and app orders, org-scoped, no N+1
- Full history is the same endpoint with `export=1` (paginated). Gated by existing `orders.read` — catalog has no CRM permission
- Cross-tenant phone/history is **404**
- `customers` module still gates the CRM surface

Not done (blockers / follow-on):

- Duplicate merge — no API, model, or Vineet merge policy in tree. Inventing it would be speculative. Stopped.
- Restaurant/retail share-identity org flag — `Organization` has no such field. Same-org locations already share this read. Do not invent the flag.
- Rewards / points (OE-220), FE CRM rebuild, second CRM engine

Out of scope: live `*.ordereasy.win`, Jira Done, deploy.
