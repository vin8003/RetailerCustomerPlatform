---
id: OE-283
title: F-0019 follow-on — fractional_children on search + POS no_page
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/pack-children-reads.md
  - docs/requirements/parent-child-pack-skus.md
  - docs/requirements/saleable-quantity-reads.md
jira: https://vin8003.atlassian.net/browse/OE-283
---

# OE-283 F-0019 follow-on — pack children on search + POS no_page

Work snapshot. Durable: [pack-children-reads.md](../requirements/pack-children-reads.md).

OE-191 shipped retailer list + detail `fractional_children`. CTO overnight lock is this EXTEND only: retailer GET search and POS `no_page` return the same child shape (`id`, `name`, `conversion_factor`, `saleable_quantity`). Non-parent → `[]`. Cross-tenant → **404** / absent. No BOM / assemble write.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Search + POS no_page show pack children | OE-191 deferred those surfaces | **EXTEND** — same shape as list/detail |
| Non-parent → `[]` | N/A on those surfaces | **EXTEND** |
| Tenant A cannot read tenant B | Search/POS already shop-scoped | **EXTEND** — absent; detail still **404** |
| Kit/BOM tables / assemble write | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- `fractional_children` on retailer product search and POS `no_page`
- Reuses `FractionalChildrenReadMixin` / `fractional_children_payload` + `saleable_quantity()`; no new service layer
- Prefetch on those reads (no per-parent child query)
- Tests: search, POS no_page, empty, tenancy, auth, query budget

Out of scope: full F-0019, OE-159, explode-at-sale, notify, FE redesign, merge, Jira Done, live `*.ordereasy.win`.
