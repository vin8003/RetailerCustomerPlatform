---
id: OE-366
title: Thin BE — optional is_serialized on product serializer if field exists
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/product-list-is-serialized.md
jira: https://vin8003.atlassian.net/browse/OE-366
---

# OE-366 Thin BE — optional is_serialized on product serializer

Work snapshot. Durable: [product-list-is-serialized.md](../requirements/product-list-is-serialized.md).

Product has no `is_serialized` column. List/detail may echo a dummy or future instance attribute without adding Meta.fields or a migration. Search Meta and POS `no_page` stay unchanged.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| List/detail include `is_serialized` when attribute exists | Absent | **EXTEND** — `hasattr` / `getattr` in `to_representation` |
| Missing attribute | n/a | key omitted (do not invent `false`) |
| False stays false | n/a | key present, value `false` |
| Search Meta / POS no_page | Existing | unchanged |

## Done in this change (EXTEND, one slice)

- `OptionalIsSerializedMixin` on `ProductListSerializer` and `ProductDetailSerializer`
- Tests: omit when missing, dummy true/false/null echo, auth denied, shop-scoped tenancy, query budget, search/POS stay without the key

Out of scope: model/migration, search Meta, POS `products/views.py`, create/update, cart, returns, ledger, FE, merge, Jira Done, live `*.ordereasy.win`.
