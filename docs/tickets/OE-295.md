---
id: OE-295
title: F follow-on — product_group on search + POS no_page
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/search-product-group.md
jira: https://vin8003.atlassian.net/browse/OE-295
---

# OE-295 F follow-on — `product_group` on search + POS no_page

Work snapshot. Durable: [search-product-group.md](../requirements/search-product-group.md).

List/detail serializers already expose top-level `Product.product_group`. `group_variants` already ships (OE-192). `ProductSearchSerializer` Meta.fields and the POS `no_page` hand-built row omitted the identity string after OE-287/290/291/293/294. This slice only echoes top-level `product_group` on search and POS `no_page`.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Search row includes `product_group` | List/detail only | **EXTEND** — same model value |
| POS `no_page` row includes `product_group` | List/detail only | **EXTEND** — same model value |
| Empty/null group stays empty/null | List/detail pass through | same on search + POS (do not invent) |
| Auth/tenancy / public catalog | Shop-scoped retailer GET | unchanged; public search inherits via shared serializer |
| Group matrix / SKU invent | Existing OE-192 variants / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- `ProductSearchSerializer` exposes `product_group` (`Product.product_group`) the same way list does (plain Meta field)
- POS `no_page` dict includes `product_group` (`p.product_group`)
- Tests: search/POS parity with list/detail, empty/null stays empty/null, public list + public search, OE-287 `brand_name` + OE-290 `barcode` + OE-291 `category_name` + OE-293 `original_price` + OE-294 `is_seasonal` light non-regression, auth denied, shop-scoped tenancy

Out of scope: group CRUD/matrix, variant SKU invent, FE, OE-102, GST rebuild, inventory.adjust, timeline/OFD/khata/UPI/slots, pack write, merge, Jira Done, live `*.ordereasy.win`.
