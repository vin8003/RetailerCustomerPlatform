---
id: OE-290
title: F follow-on — barcode on retailer search
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/search-barcode.md
jira: https://vin8003.atlassian.net/browse/OE-290
---

# OE-290 F follow-on — `barcode` on retailer search

Work snapshot. Durable: [search-barcode.md](../requirements/search-barcode.md).

List/detail/POS `no_page` already expose top-level `Product.barcode`. `ProductSearchSerializer` Meta.fields omitted it. OE-170 already matches additional barcodes in the search query; this slice only returns the primary barcode on the search response.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Search row includes `barcode` | List/detail/POS only | **EXTEND** — same model value |
| Empty/null barcode stays empty/null | List/detail/POS pass through | same on search |
| Auth/tenancy / public catalog | Shop-scoped retailer GET | unchanged; public search inherits via shared serializer |
| Barcode write / additional_barcodes echo | Existing / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- `ProductSearchSerializer` exposes `barcode` (`Product.barcode`) the same way list does
- Tests: search parity with list/detail/POS, null/empty passthrough, public list + public search, OE-287 `brand_name` light non-regression, auth denied

Out of scope: additional_barcodes echo invent, barcode write, FE, OE-102, GST rebuild, inventory.adjust, timeline/OFD/khata/UPI/slots, pack write, merge, Jira Done, live `*.ordereasy.win`.
