---
id: OE-149
title: F-0034 — Low-stock and expiry alerts
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/product-batch-expiry.md
  - docs/requirements/expiry-batch-list.md
  - docs/requirements/reorder-level-reads.md
jira: https://vin8003.atlassian.net/browse/OE-149
---

# OE-149 F-0034 — batch `is_expired` on reads (thin slice)

Work snapshot. Durable: [product-batch-expiry.md](../requirements/product-batch-expiry.md), [reorder-level-reads.md](../requirements/reorder-level-reads.md).

Jira card is the full low-stock / expiry alert capability. CTO overnight lock is this EXTEND only: retailer GET product list / detail / search / POS `no_page` (and OE-210 expiring-batches, which reuses `ProductBatchSerializer`) returns each batch with boolean `is_expired` from `ProductBatch.is_expired()`. Null / missing `expiry_date` → `false`. Auth/tenancy unchanged.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Batch payloads show expired vs not | Date only (`expiry_date`) | **EXTEND** — boolean `is_expired` |
| Null expiry is not expired | Model helper already false | **EXTEND** — echo false |
| POS `no_page` parity with serializer | Hand-built dict omitted flag | **EXTEND** — same helper |
| Tenant A cannot read tenant B | Shop-scoped product get | unchanged |
| Unauthenticated / customer denied | 401 / 403 | unchanged |
| Push/email/SMS, low-stock threshold, notify, FE dashboards | Missing / other tickets | still out of scope |

## Done in this change (EXTEND, one slice)

- `is_expired` on `ProductBatchSerializer` (list / detail / search / OE-210)
- POS `no_page` hand-built batch dicts call the same model helper
- Tests: dated-expired, dated-future, null, inactive omitted, tenancy, auth

Out of scope for the `is_expired` slice: push/email/SMS alerts, low-stock threshold invent, notify half of F-0034, FE dashboards, merge, Jira Done, live `*.ordereasy.win`.

## Follow-on (EXTEND) — optional `reorder_level` on product reads

Jira card still owns the full low-stock / expiry alert capability. This follow-on only echoes `reorder_level` on list / detail / search **when the model field exists**. `Product` has no such column today, so the key is omitted. Implementation is `to_representation` (mixin), **not** `Meta.fields`.

| AC | Before this slice | After |
|----|-------------------|--------|
| Product reads show shop reorder threshold | Field missing; payloads have no key | **EXTEND** — echo only if `Product._meta` has `reorder_level` |
| Missing model field must not crash serializers | n/a | omit key; do not declare Meta |
| Null threshold | n/a | `null` (do not invent `0`) |
| POS `no_page` / writes / alerts | Existing / invent | still out of scope |

Tests use dummy objects / local fixtures only. Never `*.ordereasy.win`.

Out of scope: model/migration invent, Meta.fields, POS `no_page`, create/update write, `?low_stock=` rewrite, notify, FE, merge, Jira Done, live `*.ordereasy.win`.
