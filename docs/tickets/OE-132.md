---
id: OE-132
title: F-0029 — Available stock by shop
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/saleable-quantity-reads.md
  - docs/requirements/product-batch-expiry.md
jira: https://vin8003.atlassian.net/browse/OE-132
---

# OE-132 F-0029 — saleable quantity on retailer/POS reads (thin slice)

Work snapshot. Durable: [saleable-quantity-reads.md](../requirements/saleable-quantity-reads.md).

Jira card is the full ATP-by-shop foundation. CTO overnight lock is this EXTEND only: retailer/POS product reads include `saleable_quantity` from the existing helper (non-expired active lots). Gross `quantity` stays unchanged. No ATP / reservation / holds.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Retailer/POS reads show saleable qty | Helper exists; payloads only had gross `quantity` | **EXTEND** — `saleable_quantity` |
| Gross `quantity` includes expired lots | EXISTING | unchanged |
| Tenant A cannot read tenant B | Shop-scoped product gets | unchanged |
| Reservations / ATP / multi-shop | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- `saleable_quantity` on retailer list, search, detail, and POS `no_page`
- Reuses `Product.saleable_quantity()` + `cache_saleable_quantities()`; no new service layer
- Tests: expired excluded, quantity stays gross, tenancy, auth

Out of scope: full F-0029, OE-102, #103, notify, FE redesign, Jira Done, live `*.ordereasy.win`.
