---
id: OE-170
title: F-0072 — Barcode and price labels (additional barcode lookup)
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/additional-barcodes-lookup.md
jira: https://vin8003.atlassian.net/browse/OE-170
---

# OE-170 F-0072 — additional barcodes in POS/catalog lookup (thin slice)

Work snapshot. Durable: [additional-barcodes-lookup.md](../requirements/additional-barcodes-lookup.md).

Jira card is the full barcode/price-label feature. CTO overnight lock is this EXTEND only: POS + catalog barcode search/lookup resolves `Product.additional_barcodes` the same as primary `barcode` (tenant-scoped, same `icontains` match).

Done in this change (EXTEND, one slice):

- `smart_product_search` treats additional barcodes like primary barcode
- POS `GET /api/products/?search=&no_page=true` and catalog `GET /api/products/search/` hit those codes
- Public shop search stays shop-scoped
- Tests: additional hit + cross-tenant isolation

Out of scope: printer HAL, price-encoded labels, FE label designer, invent barcode formats, field rename, OE-102, GST / #103, Jira Done, live `*.ordereasy.win`.
