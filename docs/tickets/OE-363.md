---
id: OE-363
title: Thin BE — optional is_hazmat on product serializer
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/product-is-hazmat.md
jira: https://vin8003.atlassian.net/browse/OE-363
---

# OE-363 Thin BE — optional `is_hazmat` on product serializer

Work snapshot. Durable: [product-is-hazmat.md](../requirements/product-is-hazmat.md).

Product has no `is_hazmat` column. List/detail must echo the attribute when it already exists on the instance. Do not add `is_hazmat` to Meta.fields.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| List/detail include `is_hazmat` when attribute exists | Missing | **EXTEND** — hasattr/getattr echo |
| Missing attribute | n/a | omit key (do not invent `false`) |
| False / null | n/a | false stays false; null stays null |
| Search Meta / POS / model | Existing / invent | still out of scope |

## Done in this change (EXTEND, one slice)

- `ProductListSerializer` / `ProductDetailSerializer` echo `is_hazmat` via mixin `to_representation` (no Meta.fields)
- Tests: dummy missing/true/false/null, Meta lock on list/detail/search/create/update, list/detail omit on real Product, echo when dummy attribute set, search stays out of scope

Out of scope: `ProductSearchSerializer` Meta, POS `products/views.py`, model/migration, create/update, cart, returns, ledger, FE, merge, Jira Done, live `*.ordereasy.win`.
