---
id: OE-284
title: F-0071 follow-on — margin_percent on POS no_page (purchase-role)
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/purchase-margin-preview.md
  - docs/requirements/shop-staff-roles.md
jira: https://vin8003.atlassian.net/browse/OE-284
---

# OE-284 F-0071 follow-on — purchase-role `margin_percent` on POS no_page

Work snapshot. Durable: [purchase-margin-preview.md](../requirements/purchase-margin-preview.md).

OE-169 shipped purchase-role list/detail/search `margin_percent`. POS `no_page` was deliberately omitted. CTO overnight lock is this EXTEND only: purchase-role `GET /api/products/?no_page=true` includes the same field from existing OE-118/OE-169 helpers.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Purchase-role sees margin% on POS no_page | List/detail/search only (OE-169) | **EXTEND** — same `margin_percent` on hand-built POS dict |
| Cost = draft SKU `purchase_price`, else last PI | Helpers exist | reuse `last_pi_unit_costs_by_product_id` + `selling_margin_percent` |
| Missing cost → null, not 0 | Preview / catalog do this | same on POS no_page |
| Cashiers / public cannot see margin | Catalog omits field | **EXTEND** — omit on POS no_page / public |
| Tenant A cannot read tenant B | Product already shop-scoped | unchanged (absent) |

## Done in this change (EXTEND, one slice)

- Purchase-role retailer GET POS `no_page` includes `margin_percent`
- Same OE-169 math; missing cost → `null`; stored `0.00` cost is real
- Non-purchase omits the field; auth/tenancy unchanged
- One batched last-PI query (no N+1)

Out of scope: POS till block, PIN/override, threshold policy, FE redesign, merge, Jira Done, live `*.ordereasy.win`.
