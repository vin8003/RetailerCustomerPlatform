---
id: OE-310
title: F follow-on — unit on purchase invoice line items
knowledge_class: durable
owning_repo: RetailerCustomerPlatform
durable_docs:
  - docs/requirements/purchase-invoice-item-unit.md
jira: https://vin8003.atlassian.net/browse/OE-310
---

# OE-310 F follow-on — top-level `unit` on purchase invoice line items

Work snapshot. Durable: [purchase-invoice-item-unit.md](../requirements/purchase-invoice-item-unit.md).

List/detail serializers already expose `Product.unit`. Purchase invoice line items omitted it. CTO overnight lock is this EXTEND only: echo the same top-level `unit` on `PurchaseItemSerializer`.

## Gap list (scout)

| AC | Before this slice | After |
|----|-------------------|--------|
| Purchase item includes `unit` | List/detail only | **EXTEND** — same model value |
| Null/empty `unit` stays null/empty | List/detail pass through | same on PI items (no invented `piece`) |
| Auth/tenancy | Shop-scoped ERP invoices | unchanged |
| Unit conversion / write APIs | Missing | still out of scope |

## Done in this change (EXTEND, one slice)

- `PurchaseItemSerializer` exposes `unit` from `product.unit` (read-only)
- Matches list/detail for the same SKU
- Tests: parity with list/detail, empty passthrough, null product, write ignore, auth, tenancy, pack identity without conversion, no extra product query when product is already select_related

Out of scope: unit-conversion engines, pack write, inventory.adjust, ProductSearchSerializer Meta (OE-312), POS views (OE-286), timeline/OFD/khata/UPI/slots, FE, merge, Jira Done, live `*.ordereasy.win`.
